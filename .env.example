# Global .env — copy this to the repo root and edit
MASTER_BALE_PHONE=+989XXXXXXXXX   # Master's Bale phone
SLAVE_BALE_PHONE=+989YYYYYYYYY    # Slave's Bale phone (different account)
HEADLESS=false                     # false = visible browser (needed for first login)
WIFI_IFACE=wlan0                   # WiFi card on SLAVE machine
HOTSPOT_SSID=BaleTunnel
HOTSPOT_PASS=tunnel1234
